# TEMPLATE ONLY. Never apply this file or commit actual credentials.
# Create this Secret through the cloud secret manager's Kubernetes integration.
apiVersion: v1
kind: Secret
metadata:
  name: zero-trust-gateway-secrets
type: Opaque
stringData:
  MONGO_URI: "mongodb+srv://<service-user>:<password>@<managed-cluster>/<db>?retryWrites=true&w=majority"
  TOKEN_REFERENCE_KEY: "<at-least-32-random-bytes-from-secret-manager>"
  IDENTITY_HASH_KEY: "<at-least-32-random-bytes-from-secret-manager>"
  TOKEN_REFERENCE_PREVIOUS_KEYS: "<optional-comma-separated-previous-key-during-rotation>"
  OIDC_CLIENT_ID: "<public-client-id>"
